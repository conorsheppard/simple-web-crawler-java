<configuration>
    <!-- Silence the internal logs at the start of the program's execution -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>
    <logger name="ch.qos.logback" level="ERROR"/>
    <logger name="ch.qos.logback.classic" level="ERROR"/>
    <logger name="ch.qos.logback.core" level="ERROR"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%n%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/crawler.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/crawler-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>  <!-- Keep logs for 7 days -->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/crawler.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <logger name="com.conorsheppard.crawler.SimpleWebCrawler" additivity="false">
        <level value="INFO"/>
        <appender-ref ref="CONSOLE"/>  <!-- Logs INFO messages to console -->
        <appender-ref ref="FILE"/>     <!-- Logs ERROR messages to file -->
    </logger>


    <logger name="com.conorsheppard.Application" additivity="false">
        <level value="INFO"/>
        <appender-ref ref="CONSOLE"/>  <!-- Logs INFO messages to console -->
        <appender-ref ref="FILE"/>     <!-- Logs ERROR messages to file -->
    </logger>

    <root level="ERROR">
        <appender-ref ref="FILE"/>
    </root>

</configuration>
